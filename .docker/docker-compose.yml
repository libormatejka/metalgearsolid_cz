services:

  mariadb:
    container_name: Database
    image: mariadb:11.8.1-ubi9-rc
    environment:
      - MARIADB_ROOT_PASSWORD=root
      - MARIADB_USER=root
      - MARIADB_PASSWORD=root
      - MARIADB_DATABASE=collectorboycz
      - MARIADB_ROOT_HOST=%
      - MYSQL_ROOT_HOST=%
      - MYSQL_DATABASE=collectorboycz
    volumes:
      - ./data/db/:/var/lib/mysql:delegated
    ports:
      - 3306:3306

  php:
    container_name: PHP
    build:
      context: ./..
      dockerfile: ./.docker/php/Dockerfile
    working_dir: /srv
    volumes:
      - ./../:/srv:delegated
    environment:
      - NETTE_DEBUG=true

  node:
    container_name: Node
    build:
      context: ./..
      dockerfile: ./.docker/node/Dockerfile
    working_dir: /srv
    volumes:
      - ./../:/srv:delegated
    ports:
      - "8080:8080"
    command: [ "tail", "-f", "/dev/null" ]

  nginx:
    container_name: Nginx
    image: nginx:1.19.4-alpine
    volumes:
      - ./../www:/srv/www:delegated
      - ./nginx/default.conf:/etc/nginx/templates/default.conf.template:delegated
    environment:
      - NGINX_PHP_HOST=php
    ports:
      - 80:80

  phpmyadmin:
    container_name: phpMyAdmin
    image: phpmyadmin:5.1.1-apache
    environment:
      - PMA_HOST=mariadb
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_USER=root
      - MYSQL_PASSWORD=root
    ports:
      - 10000:80
